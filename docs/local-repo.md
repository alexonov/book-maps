# Local repo

The app lives here: `~/Documents/projects/book-maps`  
GitHub: `https://github.com/alexonov/book-maps.git`

A new Cursor chat on that folder **does not** share this Project’s Context. Copy the markdown in `docs/` into the repo before you start that chat (skip `book-maps-src.tar.gz`). Open `docs/handoff.md` first.

Push when you want:

```bash
cd ~/Documents/projects/book-maps
git push -u origin HEAD
```

See [handoff](handoff.md), [deploy](deploy.md), and [project context](project-context.md).
